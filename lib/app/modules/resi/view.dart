import 'package:flutter/material.dart';

class CekResi extends StatelessWidget {
  const CekResi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("CekResi")),
    );
  }
}
