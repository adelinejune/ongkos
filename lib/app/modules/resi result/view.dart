import 'package:flutter/material.dart';

class ResiResult extends StatelessWidget {
  const ResiResult({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ResiResult")),
    );
  }
}
