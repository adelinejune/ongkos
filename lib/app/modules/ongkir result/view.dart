import 'package:flutter/material.dart';

class OngkirResult extends StatelessWidget {
  const OngkirResult({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("OngkirResult")),
    );
  }
}
